# ==================================================
# CONFIGURAÇÕES DA APLICAÇÃO - URL SHORTENER
# ==================================================

# Aplicação
spring.application.name=$urlshortener

# URLs da aplicação
app.url.base-url=http://localhost:8080
app.frontend.url=http://localhost:3000
app.mail.from=math.francisco2@gmail.com

# ==================================================
# CONFIGURAÇÕES DO MONGODB
# ==================================================

# URI completa do MongoDB (RECOMENDADA)
spring.data.mongodb.uri=mongodb://admin:123@localhost:27017/shortenerdb?authSource=admin


# Configuração de índices
spring.data.mongodb.auto-index-creation=true

# ==================================================
# CONFIGURAÇÕES DE GEOLOCALIZAÇÃO
# ==================================================

# GeoIP Configuration
geoip.database.path=src/main/resources/GeoLite2-City.mmdb

# ==================================================
# CONFIGURAÇÕES DE LOGGING
# ==================================================

# Logging configuration
logging.level.desafiourl.urlshortener.service.GeoLocationService=INFO
logging.level.org.springframework.data.mongodb=DEBUG

# ==================================================
# CONFIGURAÇÕES DE EMAIL/SMTP
# ==================================================

# Brevo SMTP (substitui MailerSend)
spring.mail.port=587
spring.mail.host=smtp-relay.brevo.com
spring.mail.username=902c0f001@smtp-brevo.com
spring.mail.password=kJHXgpBrI5faCAEs
# Propriedades SMTP para Brevo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp-relay.brevo.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ==================================================
# CONFIGURAÇÕES DO THYMELEAF
# ==================================================

# Configurações do Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.check-template-location=true
spring.thymeleaf.cache=false